// Client-side utility — does NOT call the DB
// Server-side ID generation is done in /api routes via lib/id-generator.ts

export function generateDrawingId(): string {
  // This is a placeholder — actual ID is generated server-side
  return "DI-TBD";
}
